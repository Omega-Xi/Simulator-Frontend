document.addEventListener("DOMContentLoaded",async()=>{bindLogout();activeNav("security.html");if(!await ensureAuthenticated())return;$("#passwordForm").addEventListener("submit",async e=>{e.preventDefault();try{const res=await apiFetch(`${base_url}/api/account/change-password`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({OLDPASSWORD:$("#oldPassword").value,NEWPASSWORD:$("#newPassword").value})});if(!res.ok)throw new Error(await readErrorMessage(res));showToast("Password changed. Please sign in again.","success");setTimeout(signOut,900);}catch(err){showToast(err.message,"error");}});$("#logoutAll").onclick=()=>showToast("Add /api/account/signout-all before enabling this action.","info");});